name: watchquest-memgraph

services:
  memgraph:
    image: memgraph/memgraph-mage:latest
    container_name: watchquest-memgraph
    restart: unless-stopped
    ports:
      - "7687:7687"
    volumes:
      - memgraph_data:/var/lib/memgraph

volumes:
  memgraph_data:
    driver: local
